# ============================================================================
# DipMaster Trading System - Unified Dependencies
# Version: 4.0.0
# Date: 2025-08-16
# 
# This file now uses the unified dependency management system.
# For environment-specific installations, use the dependencies/ directory:
#   - Core system: pip install -r dependencies/requirements.txt
#   - ML pipeline: pip install -r dependencies/requirements-ml.txt  
#   - Development: pip install -r dependencies/requirements-dev.txt
#   - Production:  pip install -r dependencies/requirements-prod.txt
# ============================================================================

# Include unified core dependencies
-r dependencies/requirements.txt

# Include ML dependencies (most common use case)
-r dependencies/requirements-ml.txt

# Version constraints for conflict resolution
-c dependencies/constraints.txt

# ============================================================================
# LEGACY COMPATIBILITY NOTICE
# ============================================================================
# This file maintains compatibility with existing deployment scripts.
# For new deployments, consider using the environment-specific files:
#
# Development setup:
#   python dependencies/setup_env.py --env-type dev
#
# Production setup:  
#   python dependencies/setup_env.py --env-type prod
#
# Testing setup:
#   python dependencies/setup_env.py --env-type testing
# ============================================================================